import { Download, Upload } from 'lucide-react';
const KEYS=['fibrapro-darkfiber','fibrapro-clientes','fibrapro-cabos','fibrapro-fusoes','fibrapro-arquivos-meta'];
export default function Configuracoes(){function exp(){const d={};KEYS.forEach(k=>d[k]=localStorage.getItem(k));const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(d,null,2)],{type:'application/json'}));a.download='fibrapro-backup.json';a.click()} function imp(e){const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{const d=JSON.parse(r.result);KEYS.forEach(k=>{if(d[k])localStorage.setItem(k,d[k])});alert('Backup importado. Recarregue a página.')}catch{alert('Backup inválido')}};r.readAsText(f)} return <div className="modulo-page"><header className="modulo-header"><div><span className="pagina-identificacao">SEGURANÇA DOS DADOS</span><h1>Configurações</h1><p>Backup dos módulos locais do FibraPRO.</p></div></header><div className="modulo-grid"><article className="modulo-card"><Download/><h2>Exportar backup</h2><p>Salva os cadastros locais em um arquivo JSON.</p><button className="botao-primario" onClick={exp}>Exportar</button></article><article className="modulo-card"><Upload/><h2>Importar backup</h2><p>Restaura um arquivo exportado anteriormente.</p><input type="file" accept=".json" onChange={imp}/></article></div></div>}
