import { Network, Boxes, Cable, Scissors, Building2, Activity } from 'lucide-react';
import { rotasBackbone } from '../data/rotas';
import { lerDados } from '../utils/localData';
export default function Dashboard(){const cards=[['Rotas Backbone',rotasBackbone.length,Network],['Cabos',lerDados('fibrapro-cabos').length,Cable],['Fusões',lerDados('fibrapro-fusoes').length,Scissors],['Dark Fiber',lerDados('fibrapro-darkfiber').length,Activity],['Clientes Dedicados',lerDados('fibrapro-clientes').length,Building2],['Arquivos',lerDados('fibrapro-arquivos-meta').length,Boxes]];return <div className="modulo-page"><header className="modulo-header"><div><span className="pagina-identificacao">NORTH TECNOLOGIA</span><h1>Centro de Operações da Rede Óptica</h1><p>Visão integrada do backbone, fibras, clientes e documentação técnica.</p></div></header><div className="modulo-grid">{cards.map(([t,n,I])=><article className="modulo-card" key={t}><div className="modulo-card-icon"><I/></div><h2>{t}</h2><strong style={{fontSize:36,color:'#fff'}}>{n}</strong><p>registros cadastrados</p></article>)}</div></div>}
